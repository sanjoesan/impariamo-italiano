name: CI – Qualitätssicherung

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Node einrichten
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Abhängigkeiten installieren
        run: npm ci

      - name: Syntax-Check
        run: npm run check

      - name: Tests (jsdom-Integration, Daten- & CSS-Hygiene)
        run: npm test
